// shoutbox.js
function loadMessages() {
    // AJAX request to load messages from server
    // Example: fetch messages from a database or a server-side script
    // and append them to the shoutbox-messages div
}

function sendMessage() {
    var name = document.getElementById('shoutbox-name').value;
    var message = document.getElementById('shoutbox-message').value;

    // AJAX request to send message to server
    // Example: send message to a server-side script to process and store
    // in a database
}
